'use strict';

/**
 * convite-newsletter router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::convite-newsletter.convite-newsletter');
